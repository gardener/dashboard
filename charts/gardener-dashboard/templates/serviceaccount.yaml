apiVersion: v1
kind: ServiceAccount
metadata:
  name: gardener-dashboard
  namespace: garden
  chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
  release: "{{ .Release.Name }}"
  heritage: "{{ .Release.Service }}"