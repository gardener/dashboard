apiVersion: {{ include "ingressversion" . }}
kind: Ingress
metadata:
  name: gardener-dashboard-ingress
  namespace: {{ .Release.Namespace }}
  labels:
    app: gardener-dashboard
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  {{- with .Values.ingress.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
  {{- end }}
spec:
  tls:
  - secretName: {{ .Values.tlsSecretName | default "gardener-dashboard-tls" }}
    hosts:
    {{- range .Values.hosts }}
    - {{ . }}
    {{- end }}
  rules:
  {{- range .Values.hosts }}
  - host: {{ . }}
    http:
      paths:
      - backend:
          serviceName: gardener-dashboard-service
          servicePort: {{ $.Values.servicePort }}
        path: /
  {{- end }}
{{- if .Values.terminal }}
{{- if .Values.terminal.gardenCluster }}
{{- if .Values.terminal.gardenCluster.kubeApiserver }}
---
apiVersion: {{ include "ingressversion" . }}
kind: Ingress
metadata:
  name: dashboard-terminal-kube-apiserver-ingress
  namespace: {{ required ".Values.terminal.gardenCluster.kubeApiserver.namespace is required" .Values.terminal.gardenCluster.kubeApiserver.namespace }}
  labels:
    app: gardener-dashboard
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  {{- with .Values.ingress.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
  {{- end }}
spec:
  tls:
  - secretName: dashboard-terminal-kube-apiserver-tls
    hosts:
    {{- range .Values.terminal.gardenCluster.kubeApiserver.hosts }}
    - {{ . }}
    {{- end }}
  rules:
  {{- range .Values.terminal.gardenCluster.kubeApiserver.hosts }}
  - host: {{ . }}
    http:
      paths:
      - backend:
          serviceName: {{ required ".Values.terminal.gardenCluster.kubeApiserver.serviceName is required" $.Values.terminal.gardenCluster.kubeApiserver.serviceName }}
          servicePort: {{ required ".Values.terminal.gardenCluster.kubeApiserver.servicePort is required" $.Values.terminal.gardenCluster.kubeApiserver.servicePort }}
        path: /
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
