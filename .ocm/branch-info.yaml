release-branch-template: hotfix-$major.$minor # e.g. hotfix-1.0
branch-policy:
  significant-part: minor # major, minor, patch
  supported-versions-count: 2
  release-cadence: null # d (days), w (weeks), y | yr (years)
